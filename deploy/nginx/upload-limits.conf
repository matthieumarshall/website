# Install this file as /etc/nginx/conf.d/oxcross-upload-limits.conf.
# nginx defaults client_max_body_size to 1 MB, which is too small for
# multipart/form-data uploads. The application enforces its own, lower limits.
client_max_body_size 25m;
